fix: correct external images loading and adjust discount filter

Frontend:
- Adjusted next.config.ts to allow localhost and 127.0.0.1 for local docker backend images
- Removed 50% discount from the filter options (now 10, 20, 30%) to be worked on later for promotional dates

Backend:
- Added get_clean_image_url helper to resolve broken external images in the database (e.g. from dummyjson)
- Applied the helper to ProductImageSerializer ensuring the Product Modal now correctly renders the gallery images instead of 404s
- Applied the helper to ProductListSerializer
